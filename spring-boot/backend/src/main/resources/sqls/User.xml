<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="me.hellonayeon.backend.user.dao.UserDao">

<select id="isExistUserId" parameterType="String" resultType="java.lang.Integer">
	select count(*)
	from user
	where id=#{id}
</select>

<insert id="createUser" parameterType="me.hellonayeon.backend.user.dto.param.CreateUserParam">
	insert into user(id, pwd, name, email)
	values(#{id}, #{pwd}, #{name}, #{email})
</insert>

<!--<select id="login" parameterType="me.hellonayeon.backend.user.domain.User"-->
<!--	resultType="me.hellonayeon.backend.user.domain.User">-->
<!--	select id, name, email-->
<!--	from user-->
<!--	where id=#{id} and pwd=#{pwd}-->
<!--</select>-->

</mapper>



